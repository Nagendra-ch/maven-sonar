pipeline {
    agent any
    stages {
        stage('---clean---') {
            steps {
             withMaven(maven : 'maven_3_5_0'){
             sh "mvn clean"
             }
             }
        }

        stage('--test--') {
            steps {
            withMaven(maven : 'maven_3_5_0'){
                sh "mvn test"
            }
            }
        }

        stage('--package--') {
            steps {
                sh "mvn package"
            }
        }
    }
}
